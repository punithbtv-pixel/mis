// Session cookie name (legacy alias kept for proxy imports).
export const AUTH_COOKIE = "ph_session";

export { SESSION_COOKIE } from "@/lib/session";
export { parseSessionToken as tokenIsValid } from "@/lib/session";
